# AI 模型配置模板
# 使用方法：复制本文件为 .env，然后填写下面的值。
# 注意：.env 已被 .gitignore 忽略，不会提交到 git，请放心填写真实 Key。

# 是否使用 mock（true=不调真实模型，false=调用真实模型）
AI_USE_MOCK=true

# 你的 API Key
AI_API_KEY=

# OpenAI 兼容的 API 地址（DeepSeek、智谱、通义等均可）
AI_BASE_URL=https://api.openai.com/v1

# 模型名称
AI_MODEL=gpt-4o-mini
