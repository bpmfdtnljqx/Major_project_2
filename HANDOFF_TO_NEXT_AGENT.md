# 交棒 Prompt — 给下一个处理这个 OJ 前端美化的 Agent

> 用法：直接把这整段作为新会话的开场 prompt 发给下一个 agent。

---

## 背景

这是一个 **Python 在线评测系统（OJ）** 课程项目（FastAPI 后端 + Streamlit 前端），主分支已基本完成功能开发。目前卡在**前端美化 / 界面观感打磨**，上一任 agent 在此陷入困境，需要你接手并谨慎处理。

- 仓库根目录：`~/python_learning/major_project_2`（WSL Ubuntu 环境，虚拟环境 `.venv`，Python 3.12）
- 后端：`app/`（FastAPI，异步）；前端：`frontend/`（Streamlit，**单页结构**：`app.py` 总控 + `views.py` 各视图函数 + `i18n.py` 双语字典 + `theme.py` 主题）
- 前端结构要点：单页主程序 `frontend/app.py`（登录门→侧边栏→顶部 radio 导航→分发到 `views.render_*`）；`views.py` 含 `render_profile/render_problems/render_solve/render_ai`；`theme.py` 负责注入全局 CSS + 提供 hero/stat_cards/section 等 HTML 卡片工具。
- 关键约束（课程硬性要求，不可破坏）：后端全部 async；前端 Streamlit；接口 `{code,msg,data}`；等等——这些已满足，**你不要破坏现有功能**。

## ⚠️ 最重要的事：你先解决"能不能看到界面"这个问题

**上一任 agent 最大的失误是：在无法看到真实界面的情况下，反复盲改 CSS + 加新功能，导致 bug 越改越多、观感混乱，用户对我失去信心。**

在这个环境里，用 agent-browser 之类的浏览器自动化去连 WSL 里的 Streamlit，**交互命令（截图/snapshot）几乎都挂起超时**，非常不可靠。所以请**务必先建立可靠的视觉反馈渠道**，再改任何视觉相关代码：

1. **首选：直接请用户提供截图**。用户会截图发你，你能识别颜色和布局。改视觉前先要图，改完再要一张确认。
2. 或：你自己能稳定截图（如果能在这个环境打通某条可靠的截图通道，就用它；打不通就老实靠用户截图）。
3. **绝不盲目改 CSS 然后让用户反复"重启+硬刷新"来碰运气**。用户对这套已经非常反感。

## 当前代码状态（重要）

- 现 HEAD 是一个"**回退后的干净状态**"（回退掉了上一任最后一次走偏的新功能——一个自己手写的"千问风格代码块组件"）。
- **但此前多轮对主题 CSS 的修改仍在历史里且**未必真正生效/未必正确。用户反馈过的观感问题**很可能仍存在且未经目视验证**。
- `theme.py` 已经被重构为 **inject() 时直接展开成具体 hex 颜色**（不再用 `var(--x)` / `color-mix()`），理论上无论 Streamlit base=dark 还是 light 都能生效——**但这只是理论，没人真正看过效果**。
- 提醒：`.streamlit/config.toml` 里的主题配置（base/backgroundColor 等）**只有重启 streamlit 进程才生效**，代码热更新不重载 config。若怀疑 config 没生效，请让用户完全重启进程（不是刷新页面）。

## 用户已反馈、但很可能还没真正修好的观感问题（按你接手时的重要性排序）

1. **亮色模式下不要出现"雪白看不清"的字**（小字/正文在亮色下对比要够，绝不用近白浅字）。
2. **未选中的 radio / 按钮**（顶部"题目/做题/…"导航、"EN/中文"语言切换）**不要显示黑色圆点**；选中态和未选中态要清楚区分、且都协调。
3. **输入框 / selectbox / 密码框 / 代码编辑器的边框**不要是突兀的纯黑粗框（亮色下尤其）。
4. **侧边栏**不要一大块深色/深蓝块；要与整体配色协调（白/浅米 或 炭蓝都行，但要干净）。
5. **做题页（render_solve）的做题区**布局与观感、以及题面里样例代码块的展示（样例别显示成黑底黑块）。
6. 全局：**代码块 / 代码编辑器**的展示要美观（用户给过"千问对话框那种代码块"作参考：顶部语言标签 + 亮暗切换 + 复制按钮 + 行号 + 语法高亮）。但**要等你能看到界面再实现**，别又盲做。

## 用户审美方向（务必遵守）

- **不要**"塑料科技感 / 一股 AI 味"：不要电光蓝紫霓虹、不要大面积玻璃拟态/发光渐变、不要那种网红 AI 视觉。
- 要**高级、克制、精致但不简约**。
- 配色参考（用户此前认过大致方向）：暗色 = 炭蓝黑底(`#0b0f14` 系) + 单点暖琥珀强调(`#e6b47a`)；亮色 = 暖白纸感底 + 琥珀强调。具体可调，但保持"低饱和、单点强调、不刺眼"。
- 用户已启用 **亮/暗主题切换**（按钮在侧边栏登出旁 + 登录页顶部，改 `session_state["ui_theme"]`），两套都要好看协调。

## 技术注意

- 前端是**单页**，不是 Streamlit 多页(pages/ 已删)。改导航/路由要小心 `st.session_state["main_nav"]`：**不要在 radio widget 实例化后再改它的 state**（会抛 `StreamlitWidgetAlreadyInstantiatedError`）；要用按钮 `on_click` 回调或在下个 run 前设置。
- i18n：所有用户可见文案尽量走 `i18n.t("...")`（zh/en），`frontend/i18n.py` 是字典。改文案别只改一半语言。
- 改代码先 `python -m py_compile` 校验；起服务用 `.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000` 和 `.venv/bin/streamlit run frontend/app.py`（**从项目根目录跑**，让 `.streamlit/config.toml` 生效）。
- git：提交用 Conventional Commits 英文；大文件不入库；**一次一小步，改完让用户看，别一口气堆一堆"盲改"**。

## 建议的第一步

先不要写任何代码。先跟用户确认：能不能给我几张当前界面的截图（暗色、亮色各来，重点：题目列表页 + 做题页 + 我的页）。拿到截图、看清现状后，再告诉我你打算怎么修、改哪些，用户点头再动手。
